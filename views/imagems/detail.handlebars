<div class="item-page">
  <h1>{{imagem.nome}}</h1>
  <img src="/img/imagem/{{imagem.imagem}}" alt="{{imagem.nome}}">
  <p>Descrição: {{imagem.descricao}}</p>
  <a class="button" href="/imagems">Voltar</a>
  <form style="display:inline-block; margin: 2px;" action="/imagems/remove/{{imagem._id}}" method="POST">
    <input type="submit" class="button" value="Excluir">
  </form>
</div>




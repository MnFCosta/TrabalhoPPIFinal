<div class="item-page">
  <h1>{{receita.nome}}</h1>
  <img src="/img/receita/{{receita.imagem}}" alt="{{receita.nome}}">
  <p>Ingredientes: {{receita.ingredientes}}</p>
  <p>Descrição: {{receita.descricao}}</p>
  <a class="button" href="/receitas">Voltar</a>
  <form style="display:inline-block; margin: 2px;" action="/receitas/remove/{{receita._id}}" method="POST">
    <input type="submit" class="button" value="Excluir">
  </form>
</div>